# Un único origen para la web y la API: así la PWA llama a /api sin CORS
# y sin tener que configurar ninguna URL.
{$RATAFIT_DOMAIN} {
	encode gzip

	handle /api/* {
		reverse_proxy api:8080
	}

	handle {
		reverse_proxy web:80
	}
}
